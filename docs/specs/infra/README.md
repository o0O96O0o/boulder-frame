# Infrastructure Specifications

The infrastructure submodule defines the local Compose stack and the online self-hosted override. It does not contain application business logic.

## Documents

- [Compose Runtime](compose-runtime.md): services, networks, volumes, environment propagation, startup ordering, and direct object uploads.
