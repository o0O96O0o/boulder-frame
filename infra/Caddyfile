# External browser routes remain deliberately blocked until authentication exists.
# The health route is the only public endpoint required for online host checks.
{$CADDY_DOMAIN} {
    encode zstd gzip

    @health path /healthz
    reverse_proxy @health backend:8080

    respond "External access is disabled until authentication is implemented." 403
}
